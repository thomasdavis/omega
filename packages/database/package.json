{
  "name": "@repo/database",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc && cp -r src/postgres/migrations/*.sql dist/postgres/migrations/",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "~5.22.0",
    "@repo/shared": "workspace:*",
    "ai": "6.0.0-beta.99",
    "mongodb": "^7.0.0",
    "pg": "^8.16.3",
    "prisma": "~5.22.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.3"
  }
}
