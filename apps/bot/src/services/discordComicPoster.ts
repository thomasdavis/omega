/**
 * Discord Comic Poster Service
 * Posts generated comic images to Discord channels
 */

import { Client, TextChannel, AttachmentBuilder } from 'discord.js';

interface PostComicOptions {
  channelId: string;
  imageData: Buffer;
  prNumber: number;
  prTitle: string;
  prAuthor: string;
  prUrl: string;
  imagePath?: string;
}

interface PostComicResult {
  success: boolean;
  messageId?: string;
  error?: string;
}

/**
 * Post a comic to Discord channel
 */
export async function postComicToDiscord(
  client: Client,
  options: PostComicOptions
): Promise<PostComicResult> {
  const { channelId, imageData, prNumber, prTitle, prAuthor, prUrl, imagePath } = options;

  try {
    console.log(`📤 Posting comic to Discord channel ${channelId}`);

    // Fetch the channel
    const channel = await client.channels.fetch(channelId);

    if (!channel) {
      console.error(`❌ Channel ${channelId} not found`);
      return {
        success: false,
        error: `Channel ${channelId} not found`,
      };
    }

    if (!channel.isTextBased() || !(channel instanceof TextChannel)) {
      console.error(`❌ Channel ${channelId} is not a text channel`);
      return {
        success: false,
        error: `Channel ${channelId} is not a text channel`,
      };
    }

    // Create attachment from image data
    const attachment = new AttachmentBuilder(imageData, {
      name: `pr-${prNumber}-comic.png`,
    });

    // Create plain text message with PR information
    const plainTextMessage = `🎨 **Comic: PR #${prNumber}**\n${prTitle}\n\n👤 Author: ${prAuthor}\n🔗 Pull Request: ${prUrl}\n\n_Generated by Gemini API_`;

    // Send the message
    const message = await channel.send({
      content: plainTextMessage,
      files: [attachment],
    });

    console.log(`✅ Comic posted to Discord (message ID: ${message.id})`);

    return {
      success: true,
      messageId: message.id,
    };
  } catch (error) {
    console.error('❌ Error posting comic to Discord:', error);
    return {
      success: false,
      error: error instanceof Error ? error.message : String(error),
    };
  }
}

/**
 * Get Discord client instance
 * This assumes the bot is already connected
 */
export function getDiscordClient(): Client | null {
  // In a real implementation, this would access the global client instance
  // For now, we'll return null and handle it in the caller
  return null;
}
