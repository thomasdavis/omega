name: CI Checks

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Build and Test
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        if: github.event_name == 'pull_request'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.0.0

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install dependencies (with lockfile auto-fix)
        id: install
        run: |
          echo "Attempting install with frozen lockfile..."
          if pnpm install --frozen-lockfile 2>&1 | tee install-output.txt; then
            echo "✅ Lockfile is in sync"
            echo "lockfile_fixed=false" >> $GITHUB_OUTPUT
          else
            echo "⚠️  Lockfile out of sync, regenerating..."
            echo "lockfile_fixed=true" >> $GITHUB_OUTPUT

            # Regenerate lockfile
            pnpm install --no-frozen-lockfile

            # Only commit if this is a PR
            if [ "${{ github.event_name }}" == "pull_request" ]; then
              # Check if lockfile was modified
              if git diff --quiet pnpm-lock.yaml; then
                echo "ℹ️  No lockfile changes needed after all"
                echo "lockfile_fixed=false" >> $GITHUB_OUTPUT
              else
                echo "✅ Lockfile regenerated successfully"

                # Commit and push the updated lockfile
                git add pnpm-lock.yaml
                git commit -m "chore: auto-fix pnpm lockfile" -m "Lockfile was out of sync with package.json changes." -m "Auto-fixed by CI workflow." -m "🤖 Generated by GitHub Actions"
                git push origin HEAD:${{ github.event.pull_request.head.ref }}
                echo "✅ Lockfile committed and pushed"
              fi
            fi
          fi

      - name: Comment on lockfile fix
        if: steps.install.outputs.lockfile_fixed == 'true' && github.event_name == 'pull_request' && startsWith(github.event.pull_request.head.ref, 'claude/')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_NUMBER="${{ github.event.pull_request.number }}"
          gh pr comment "$PR_NUMBER" --body "$(cat <<'EOF'
          🔧 **Lockfile Auto-Fixed**

          The pnpm-lock.yaml was out of sync with package.json changes.

          ✅ Automatically regenerated and committed the lockfile.
          🔄 CI checks will re-run with the updated lockfile.
          EOF
          )"

      - name: Type check
        run: pnpm type-check

      - name: Build
        run: pnpm build

      - name: Get commit info
        id: commit
        run: |
          echo "message=$(git log -1 --pretty=%B | head -1)" >> $GITHUB_OUTPUT
          echo "author=$(git log -1 --pretty=%an)" >> $GITHUB_OUTPUT
          echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Notify Discord - Build Success
        if: success() && github.event_name == 'push'
        uses: tsickert/discord-webhook@v6.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          embed-title: "✅ Build Passed"
          embed-description: |
            **Commit:** ${{ steps.commit.outputs.message }}
            **Author:** ${{ steps.commit.outputs.author }}
            **SHA:** `${{ steps.commit.outputs.sha }}`

            All checks passed! Railway is deploying automatically.
            Check deployment status at: https://railway.app/project/211e3c65-73ad-4e79-8b74-ff3762fcda73
          embed-color: 5814783
          embed-url: https://github.com/${{ github.repository }}/commit/${{ github.sha }}

      - name: Notify Discord - Build Failed
        if: failure() && github.event_name == 'push'
        uses: tsickert/discord-webhook@v6.0.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          embed-title: "❌ Build Failed!"
          embed-description: |
            **Commit:** ${{ steps.commit.outputs.message }}
            **Author:** ${{ steps.commit.outputs.author }}
            **SHA:** `${{ steps.commit.outputs.sha }}`

            Build or type check failed. Railway deployment blocked.
          embed-color: 15158332
          embed-url: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
