{
  "name": "discord-ai-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless Discord AI bot using OpenAI, Vercel AI SDK v6, and Discord Interactions API",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
